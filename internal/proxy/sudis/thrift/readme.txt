Я установил thrift по инструкции отсюда 'https://thrift.apache.org/docs/BuildingFromSource'

Потом сгенерировал код для go командой 'thrift -r --gen go cciSpAuth.thrift'

Вся остальная инициализация сервиса в main на основе сгенеренного кода
